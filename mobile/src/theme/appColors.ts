/** Brand palette — use these across the app instead of one-off hex values. */
export const APP_LIME = "#C9FF35";
export const APP_BLACK = "#262626";

/** Primary dark surface (feed, headers, full-screen viewers). */
export const APP_DARK_BG = APP_BLACK;

/** Slightly elevated surface on dark backgrounds (cards, inputs). */
export const APP_SURFACE = "#303030";

/** Muted lime for inactive dots / subtle fills. */
export const APP_LIME_MUTED = "rgba(201, 255, 53, 0.24)";

/** Primary text on dark backgrounds. */
export const APP_TEXT = "#f8fafc";

/** Secondary text on dark backgrounds. */
export const APP_TEXT_MUTED = "#a8b0b8";

/** Dark text on lime buttons / inverted slides. */
export const APP_TEXT_ON_LIME = APP_BLACK;
